var path = require('path');
var expect = require('chai').expect;

var {pigLatin} = require(path.join(__dirname, '..', './pigLatin.js'));

describe('pigLatin()', function () {
  'use strict';

  it('returns "efday" when "def" is input', function () {
    
  });
});
